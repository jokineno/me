---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "absolute rounded-full opacity-30 blur-3xl bg-gradient-to-br from-iso-300 via-iso-400 to-iso-200",
    className,
  ]}
  aria-hidden="true"
/>
